import "./styles.css";
import Header from "./components/Header";

export default function App() {
  return (
    <div className="App">
      <Header />
      <h1>Hello CodeSandbox</h1>
      <h2>Start editing to see some magic happen!</h2>
      <h2>Hello {process.env.NAME}</h2>
      <h2>This directly came form the updated repository</h2>
      <h2>if this will also work now</h2>
      <h2>It will work more faster now</h2>
    </div>
  );
}

// so this is the header file for this application
// so this directly came from the components directory right or alright
// if this will get updated in the visual studio code also
// and this will also get updated on the visual studio code right vishal sharma
// this is also not working fine right working no
// my version won
